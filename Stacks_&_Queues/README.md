# Stacks and Queues

## Short Quiz on Stacks & Queues in Python

### Choose the correct answer for the questions below 

```
Q. This form of access is used to add and remove nodes from a queue.
- LIFO, Last In First Out
- FIFO, First In First Out
- none
```

```
Q. Which of the following usees FIFO method
- Queue
- Stack
- linklist
```

```
Q.  ........ form of access is used to add remove nodes from a stack.
- FIFO
- LIFO
- ALL
```

```
Q. Consider the following operation performed on a stack of size 5.
Push(1);
Pop();
Push(2);
Push(3);
Pop();
Push(4);
Pop();
Pop();
Push(5);

After the completion of all operation, the number of elements present on stack are:
- 1
- 2
- 3
- 4
```

```
Q. What method is used to add an element to a Queue?
- dequeue()
- enqueue()
- push()
```

```
Q. What method is used to add an element to a Stack?
- dequeue()
- enqueue()
- push()
```

```
Q. What method is used to view the next element to be removed from a stack?
- NextItem()
- dequeue()
- peek()
- pop()
```

```
Q. Which method is called in a PUSH() method
- IsEmpty()
- IsFull()
```

```
Q. Which method is called in a POP() method
- IsEmpty()
- IsFull()
```

```
Q.  If I want to find out if an item is in a queue or a stack - what do I call?
- Contains(item)
- Peek(item)
- IsContains(item)
- Find(item)
```

```
Q.  If the element "A B C D" are placed in a stack and are deleted one at a time
in what order will they be removed?
- ABCD
- DCBA
- DBCA
- DABC
```